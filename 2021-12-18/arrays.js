const subjects = ["Maths", "English", "Hindi", "SSC", "French", "Science"]

console.log(subjects)

// index => postion in an array
// index starts with 0 and goes until length-1 of array

let len = subjects.length
console.log("No of subjetcs = "+len)

console.log(subjects[3])

for( let i=0; i<len; i++){
	console.log(i)
}